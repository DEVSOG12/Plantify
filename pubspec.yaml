name: plantify
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  introduction_screen:
  tflite:
  image_picker:
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.9.1
  # flutter_launcher_name:

# flutter_launcher_name:
#   name: "Plantify"


flutter_icons:
  android: "ic_launcher"
  ios: true
  image_path: "assets/final_logo.jpg"


flutter:

  uses-material-design: true

  assets:
    - assets/
